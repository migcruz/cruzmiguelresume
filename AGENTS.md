# Agent Context

Read this file before making any changes. It captures architectural decisions, gotchas, and current project state so you can continue work without re-deriving context from the code.

---

## What this project is

An HTML/CSS resume **and cover letter** for **Miguel Cruz, Embedded Systems Engineer**. All content lives in typed TypeScript files under `src/resume/` and `src/cover-letter/`. Each `index.html` is a thin shell that JavaScript populates at load time. Chrome headless (Puppeteer) generates the PDFs so the browser live preview and the PDF output share the same rendering engine.

PDFs produced:
- **`MiguelCruz_Resume.pdf`** — full personal details (`profile=real`)
- **`JohnDoe_Resume.pdf`** — anonymized version (`profile=anon`)
- **`MiguelCruz_CoverLetter.pdf`** — cover letter (`profile=real`), local build only

---

## File responsibilities

| File | Purpose |
|---|---|
| `src/style.css` | **Shared** — all layout, fonts, colors, print styles for both resume and cover letter |
| `src/fonts/` | Self-hosted TTF files (geist-sans/, cormorant-garamond/) |
| `src/resume/content.ts` | **Source** — all resume data with TypeScript interfaces. Edit content here |
| `src/resume/content.js` | **Compiled artifact** — generated by `tsc`, loaded by the browser via `<script>` tag |
| `src/resume/index.html` | Thin shell — empty containers only, no inline content. Loads `content.js` and runs `populate()` |
| `src/cover-letter/content.ts` | **Source** — cover letter data (recipient, date, paragraphs). Edit content here |
| `src/cover-letter/content.js` | **Compiled artifact** — generated by `tsc`, loaded by browser |
| `src/cover-letter/index.html` | Thin shell for cover letter — same header/footer as resume |
| `src/cover-letter/style.css` | Cover letter body styles only — extends the shared `../style.css` |
| `generate-resume.ts` | **Source** — Puppeteer script, loads `src/resume/index.html?profile=<X>` and calls `page.pdf()` |
| `generate-resume.js` | **Compiled artifact** — generated by `tsc`, executed by Docker |
| `generate-cover-letter.ts` | **Source** — Puppeteer script for cover letter, loads `src/cover-letter/index.html` |
| `generate-cover-letter.js` | **Compiled artifact** — generated by `tsc`, executed by Docker |
| `package.json` | Node deps: `puppeteer-core` (runtime), `typescript` + `@types/node` (dev) |
| `tsconfig.json` | TypeScript config — `strict: true`, `module: CommonJS`, no `outDir` (emits next to source) |
| `Dockerfile` | `node:20-slim` + Chromium; runs `npm install && npm run build` then `node generate-resume.js` |
| `docker-compose.yml` | Services: `resume-real`, `resume-anon`, `cover-letter-real`, `tsc-watch` (dev profile) |
| `README.md` | User-facing docs with usage, Mermaid diagram, and TODO list |
| `AGENTS.md` | This file — agent handoff context |

---

## Content architecture

### Resume profiles (`src/resume/content.ts`)

```
profiles.real = {
  meta:       { title, pdfFilename }
  name:       string
  contact:    { email, phone, location, github: { handle, url }, linkedin: { handle, url } }
  summary:    string
  education:  [{ degree, credential, institution, dates }]
  skills:     [{ label, items: [...] }]           // 6 groups
  experience: [{ title, company, type, dates, location, bullets: [...], anonBullets?: [...] }]
  footer:     { name, linkedin: { label, url } }
}
```

`profiles.anon` uses object spread over `profiles.real`, overriding only:
- `meta` (title, filename)
- `name`
- `contact` (all fields)
- `education` — mapped to override `institution` and `dates`
- `experience` — mapped to override `company` and `location`; uses `anonBullets` when present
- `footer`

### anonBullets pattern

If a job entry's bullet text contains identifying company/product names, add an `anonBullets` array alongside `bullets` on that entry. The anon profile map picks up `anonBullets` if present and falls back to `bullets` otherwise:

```ts
bullets: job.anonBullets ?? job.bullets
```

### Cover letter profiles (`src/cover-letter/content.ts`)

```
coverLetterProfiles.real = {
  meta:       { title, pdfFilename }
  name:       string
  contact:    { email, phone, location, github: { handle, url }, linkedin: { handle, url } }
  footer:     { name, linkedin: { label, url } }
  date:       string                              // displayed as the section h2 in main column
  recipient:  { company, address, location, phone, email }
  greeting:   string
  paragraphs: string[]
  closing:    string
}
```

Note: `name`, `contact`, and `footer` in the cover letter are maintained manually in sync with the resume — they are not imported or shared at runtime.

### Profile switching

Each `index.html` reads `?profile=` from the URL and calls `populate(profiles[profile])` or `populate(coverLetterProfiles[profile])`. Default is `real`.

- Resume real: `src/resume/index.html`
- Resume anon: `src/resume/index.html?profile=anon`
- Cover letter: `src/cover-letter/index.html`

---

## Layout

### Resume

Two-column grid: `20% sidebar | 1fr main`.

- **Header** (full width): Name only — Cormorant Garamond, uppercase, `letter-spacing: 0.2em`
- **Sidebar** (left 20%): Contact, Education, Skills — grey background (`#f5f7f7`), `padding: 2mm`
- **Main column** (right): Professional Summary, Professional Experience — `padding-top: 2mm`
- **Footer**: `5mm` strip, name floated left, LinkedIn URL floated right

### Cover letter

Same two-column grid and header/footer as the resume.

- **Sidebar** (left 20%): Contact, Recipient — same grey background
- **Main column** (right): Date (as section `h2`), greeting, body paragraphs, closing, signature
- **Cover letter body styles** live in `src/cover-letter/style.css` — letter text is `11px` / `line-height: 1.7`

---

## CSS architecture — critical details

### Font sizing: all `px`, no `rem`
Every font size is in explicit `px`. Each section has its own clearly labelled block in `style.css` with independent font sizes you can tune without affecting others.

### Font weights
- **Body (Geist Sans):** `font-weight: 300` (Light) set on `body`, cascades to all body text
- **Name (Cormorant Garamond):** `font-weight: 400` (Regular)
- **Section h2 labels:** `font-weight: 500`
- **Entry titles / skill labels:** `font-weight: 500`

### Typography quality
Set on `body` for both screen and print:
- **`text-rendering: optimizeLegibility`** — enables kerning and common ligatures at the font rendering level
- **`font-feature-settings: "kern" 1, "liga" 1, "calt" 1`** — explicitly enables OpenType kerning, standard ligatures, and contextual alternates

### Section heading sizes are scoped with `:has()`
```css
/* Shared (style.css) */
.sidebar section:has(.contact) h2     { font-size: 14px; }
.sidebar section:has(.entry) h2       { font-size: 14px; }
.sidebar section:has(.skill-group) h2 { font-size: 14px; }
.main-col section:first-child h2      { font-size: 14px; } /* Summary / Date */
.main-col section:last-child h2       { font-size: 14px; } /* Experience */

/* Cover letter only (cover-letter/style.css) */
.sidebar section:has(.recipient-list) h2 { font-size: 14px; }
```
Chrome fully supports `:has()` — no caveat for PDF output.

### Page is fixed A4
`.page` uses `width: 210mm; min-height: 297mm` with `padding: 18mm 10mm 10mm`. This is intentional — the page should always look like A4 regardless of font size changes.

### Header is fixed height
The header uses `height: 30mm` with `text-align: center; line-height: 30mm` to center the name — **not flexbox**. `line-height: 30mm` vertically centers single-line text within the fixed-height box. Changing `font-size` on `header h1` does **not** affect the header height or push content down.

The grey background bleeds to the page top edge via the negative margin trick:
```css
header {
  margin-top: -18mm;   /* cancels .page padding-top */
  margin-left: -10mm;  /* cancels .page padding-left */
  margin-right: -10mm; /* cancels .page padding-right */
  height: 30mm;
}
```

### Print / Chrome CSS architecture

The `@media print` block has rules critical for correct PDF output:

- **`@page { margin: 0 }`** — Chrome clips all content to the `@page` margin boundary. Setting margins here would cause the header's negative-margin bleed to be clipped at the top edge, cutting off the header. By zeroing `@page` margins, the full physical page is the content area and the negative-margin header bleed works correctly.
- **`.page { padding: 18mm 10mm 10mm }` kept in print** — all page spacing is handled here, not by `@page`.
- **`generate-resume.js` / `generate-cover-letter.js` margin `{ top: 0, bottom: 0, left: 0, right: 0 }`** — Puppeteer's `page.pdf()` margin parameter is independent of the CSS `@page` rule. Both must be zero to avoid double margins.
- **`.page { display: block }`** — overrides screen `display: flex` so the grid doesn't try to stretch to fill the page in print.
- **`print-color-adjust: exact`** — ensures Chrome preserves background colors (grey sidebar, grey header).

### Fonts are self-hosted
Both fonts are loaded via `@font-face` in `src/style.css` using local TTF files. The `url()` paths in `@font-face` are relative to the CSS file's location (`src/`), not the HTML that imports it — so `url('fonts/...')` resolves correctly whether the HTML is at `src/resume/index.html` or `src/cover-letter/index.html`.

```
src/fonts/
  geist-sans/
    Geist-Light.ttf        (weight 300, normal)
    Geist-LightItalic.ttf  (weight 300, italic — needed for .entry-company)
    Geist-Regular.ttf      (weight 400, normal)
    Geist-Medium.ttf       (weight 500, normal)
  cormorant-garamond/
    CormorantGaramond-Regular.ttf  (weight 400, normal)
```

**If you add a new font weight or style, add both the TTF file and a matching `@font-face` block in `src/style.css`, then rebuild the Docker image.**

---

## Design decisions

| Element | Value | Notes |
|---|---|---|
| Name font | Cormorant Garamond 400 | Serif, centered, uppercase, `letter-spacing: 0.2em` |
| Body font | Geist Sans 300 | Light weight — deliberately thin for a clean look |
| Section h2 | Geist Sans 500 | Slightly heavier than body for contrast without being bold |
| Header | Fixed `30mm` height, `line-height` centered | No flex |
| Page size | A4 fixed (`210mm × 297mm`) | Not responsive — intentional |
| Body text size | `10–11px` range | Explicit per section, not inherited |
| Side margins | `10mm` (via `.page` padding) | Safe margin for modern printers |
| Page footer | `5mm` strip, floated spans | Name left, LinkedIn right |
| Cover letter date | Rendered as section `h2` in main column | Same visual weight as "Professional Summary" header |
| Text color | `#283135` | CMYK 69, 62, 59, 49 converted to hex |
| Grey backgrounds | `#f5f7f7` | CMYK 4, 3, 3, 0 converted to hex. Used on header and sidebar |

**Color note:** CSS does not support CMYK values. Always convert CMYK to hex before applying.

---

## Known gotchas

1. **Chrome clips content to the `@page` margin boundary** — negative margins cannot bleed into `@page` margin space. This is why `.page` keeps its padding in print and `@page { margin: 0 }` is used. Do not change this without understanding the header bleed mechanism.
2. **`generate-resume.ts` / `generate-cover-letter.ts` margin and CSS `@page` margin are independent** — Puppeteer's `margin` option and the CSS `@page` rule both contribute to page margins. Currently both are zero. If either is changed without changing the other, margins will be wrong.
3. **`waitUntil: 'networkidle0'`** — required in both generate scripts so Puppeteer waits for local font files to finish loading before capturing the PDF. Using `load` or `domcontentloaded` may capture the page before fonts are applied.
4. **`--no-sandbox` in Puppeteer launch args** — required when Chromium runs as root inside a Docker container. Without it the container exits immediately with a permissions error.
5. **`printBackground: true`** — required for Puppeteer to preserve background colors. Chrome's default print behavior strips backgrounds. This flag overrides that.
6. **`file://` URL in generate scripts** — Puppeteer loads pages as local file URLs. This works because `content.js` and fonts are on the same filesystem. No local HTTP server is needed.
7. **`var` not `const` for globals in `content.ts` files** — `const` at the top level of a browser script goes into the global *lexical* environment but does NOT become a property of `window`/`globalThis`. `page.evaluate` callbacks access globals via `globalThis`, so `globalThis.profiles` would be `undefined` with `const`. `var` at the top level DOES create a `window` property. This is why `profiles` and `coverLetterProfiles` are declared with `var`. Do not change them to `const`.
8. **Compiled `.js` files are artifacts** — do not edit `content.js`, `generate-resume.js`, or `generate-cover-letter.js` directly. Edit the `.ts` source files and run `npm run build` (or let Docker do it).
9. **Live preview requires compiled JS on the host filesystem** — `src/resume/content.js` and `src/cover-letter/content.js` must exist locally before opening the HTML files in Live Server. Use `docker compose --profile dev up --build tsc-watch` (the `--build` flag is required — `tsconfig.json` is baked into the image and must be rebuilt when it changes). The `tsc-watch` service mounts `./src:/app/src`, so compiled output is written back to the host through the volume.
10. **`section:first-child` / `section:last-child`** — In the resume's `.main-col`, Summary is targeted as `:first-child` and Experience as `:last-child`. If a new section is added between them, these selectors will break. Add explicit classes if needed. In the cover letter, the date section is the only section in `.main-col`, so both selectors apply to it — this is fine.
11. **`.main-col` padding-top** — Set to `2mm` to align the first section with the Contact section in the sidebar (which has `padding: 2mm`). If sidebar padding changes, update `.main-col { padding-top }` to match.
12. **`min-height` on `.page`** — If content overflows A4, a second page is created. Page breaks are managed via `page-break-inside: avoid` on `.entry` only. Do not add `page-break-inside: avoid` to `section` — it forces entire sections onto a single page as an unbreakable block.
13. **Contact icons are inline SVG string constants** — defined as `GITHUB_ICON` and `LINKEDIN_ICON` at the top of the inline `<script>` in each `index.html`. If you need to update an icon, change the constant in both `src/resume/index.html` and `src/cover-letter/index.html`.
14. **Font `url()` paths are relative to `src/style.css`, not the HTML** — Both resume and cover letter HTML files load `../style.css` (one level up from their subfolder). The `url('fonts/...')` paths inside the CSS resolve relative to the CSS file's location (`src/`), which is correct. Do not move `style.css` without updating all font paths.
15. **Cover letter `name`/`contact`/`footer` are not shared with the resume** — They are duplicated in `src/cover-letter/content.ts` and must be kept in sync manually. There is no runtime import between the two content files.

---

## TODO

1. ~~**Separate content from markup**~~ — **Done.** All data lives in typed TypeScript files.
2. ~~**CI/CD with GitHub Actions**~~ — **Done.** Resume PDFs are generated and attached as release artifacts on push to `main`.
3. ~~**Anonymized resume variant**~~ — **Done.** `JohnDoe_Resume.pdf` generated via `profile=anon`.
4. ~~**Self-hosted fonts**~~ — **Done.** Geist Sans and Cormorant Garamond TTFs bundled in `src/fonts/`.
5. ~~**TypeScript migration**~~ — **Done.** All source files fully typed with strict mode.
6. ~~**Cover letter**~~ — **Done.** Matching header/footer/sidebar styling, recipient section, date as section header.
7. **PDF hot-reload watcher** — Add a `watch` service to `docker-compose.yml` that monitors source files and regenerates the PDFs automatically.
8. **Content validation** — Pre-build step to validate content data against TypeScript interfaces (required fields present, dates formatted correctly, bullet points under a character limit).
